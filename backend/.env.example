DATABASE_URL="postgresql://ticketing:ticketing_pass@localhost:5432/ticketing_db?schema=public"
JWT_SECRET="ganti-dengan-random-string-yang-panjang"
PORT=3000
FRONTEND_URL="http://localhost:5173"
APP_URL="http://localhost:5173"

# --- Email (SMTP) ---
# Kalau SMTP_ENABLED=false, email cuma di-log ke console (aman untuk development tanpa SMTP asli)
SMTP_ENABLED=false
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="[email protected]"
SMTP_PASS="app-password-kamu"
MAIL_FROM="Ticketing System <[email protected]>"

# --- File Upload ---
UPLOAD_DIR="./uploads"
